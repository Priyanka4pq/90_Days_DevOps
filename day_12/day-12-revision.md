
# Day 12 – Revision (Days 01–11)

## Objective

Today was a revision day focused on reinforcing the Linux and DevOps fundamentals learned during Days 01–11. I reviewed commands, reran important exercises, and identified areas to improve.

---

# Revision Summary

## Day 01 – Mindset & Learning Plan

- Reviewed my DevOps learning roadmap.
- Goal remains the same: build strong Linux, Docker, Kubernetes, CI/CD, Cloud, and Monitoring skills.
- Continue practicing commands daily instead of only reading notes.

---

## Processes & Services Review

Commands practiced:

```bash
ps aux
```

Observation:
- Displays all running processes along with CPU and memory usage.

```bash
systemctl status nginx
```

Observation:
- Shows whether the Nginx service is active, inactive, or failed.

```bash
journalctl -u nginx
```

Observation:
- Displays logs generated by the Nginx service, useful for troubleshooting.

---

## File Operations Practice

Commands practiced:

```bash
echo "Revision Day" >> notes.txt
```

```bash
chmod 755 script.sh
```

```bash
ls -l
```

Observation:
- Successfully appended text to a file.
- Changed file permissions.
- Verified permissions using `ls -l`.

---

## Cheat Sheet Refresh

Five Linux commands I would use first during troubleshooting:

- `ls -l` – Check files and permissions.
- `pwd` – Verify current directory.
- `cd` – Navigate quickly.
- `ps aux` – View running processes.
- `systemctl status` – Check service status.

---

## User & Group Practice

Commands practiced:

```bash
sudo useradd testuser
```

```bash
id testuser
```

```bash
sudo chown testuser devops.txt
```

Verified ownership using:

```bash
ls -l devops.txt
```

---

# Mini Self-Check

## 1. Which 3 commands save you the most time right now, and why?

- `ls -l` – Quickly checks files and permissions.
- `systemctl status` – Instantly checks whether a service is running.
- `ps aux` – Helps identify running processes and troubleshoot issues.

---

## 2. How do you check if a service is healthy?

Commands:

```bash
systemctl status nginx
```

```bash
journalctl -u nginx
```

```bash
ps aux | grep nginx
```

---

## 3. How do you safely change ownership and permissions?

Example:

```bash
sudo chown ubuntu:developers app.log
```

```bash
chmod 640 app.log
```

Always verify using:

```bash
ls -l app.log
```

---

## 4. What will you focus on improving in the next 3 days?

- Practice Linux commands daily.
- Strengthen understanding of file permissions and ownership.
- Learn more about networking and shell scripting.

---

# Key Takeaways

- Linux commands become easier with daily practice.
- File permissions and ownership are essential for system security.
- Service management is an important skill for troubleshooting.
- Revision helps reinforce concepts and improves confidence.
- Practical exercises are more effective than only reading notes.

---
